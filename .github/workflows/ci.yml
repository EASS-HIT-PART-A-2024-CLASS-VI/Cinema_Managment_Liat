name: Cinema Management System CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-mock pytest-asyncio fastapi sqlalchemy httpx bcrypt
        
        # Install frontend dependencies
        if [ -f frontend/requirements.txt ]; then pip install -r frontend/requirements.txt; fi
        
        # Install backend dependencies
        if [ -f backend/app/requirements.txt ]; then pip install -r backend/app/requirements.txt; fi
        
        # Install LLM service dependencies
        if [ -f backend/llm_service/requirements.txt ]; then pip install -r backend/llm_service/requirements.txt; fi
    
    - name: Install frontend package in development mode
      run: |
        cd frontend
        pip install -e .
    
    - name: Set PYTHONPATH
      run: |
        echo "PYTHONPATH=$PYTHONPATH:${{ github.workspace }}" >> $GITHUB_ENV
    
    - name: Run Frontend Tests
      run: |
        cd frontend
        pytest tests/ -v
    
    - name: Run Backend Integration Tests
      run: |
        python -m pytest tests/backend -v
    
    - name: Lint code
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics